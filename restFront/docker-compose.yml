version: '3'

services:
  web:
    container_name: restf
    image: "docker.pkg.github.com/dschek/restf/restf:latest"
    ports:
      - "9001:80"
networks:
  default:
    external:           
      name: local