services:
  web:
    container_name: nginx
    image: "registry.gitlab.com/mrcod/nginx:latest"
    ports:
      - "80:80"
    networks:
      - local
      - nginx
    restart: always
networks:
  nginx:
    name: nginx
    external: true
  local:
    name: local
    external: true
